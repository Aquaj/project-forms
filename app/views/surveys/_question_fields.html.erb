 <div class="nested-fields"> <%= f.input :content %>
    <%= f.input :position %>

  <%= link_to_remove_association f, data: {confirm: 'Are you sure?'} do %>
    X
  <% end %>
  <div class="choices">
    <%= f.fields_for :choices do |choice| %>
      <%= render 'choice_fields', f: choice %>
    <% end %>
    <div class="links">
        <%= link_to_add_association 'add choice', f, :choices %>
    </div>
  </div>
</div>